# Nixpacks — Node.js 20 para el frontend
# @tailwindcss/oxide y workbox-build requieren Node >= 20
providers = ["node"]

[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npx serve dist -s -l $PORT"
